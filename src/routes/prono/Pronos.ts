import type { Game } from "$lib/types/game";
import type { Prono } from "$lib/types/prono";
import { fetchError } from "./store";

export async function getPronosWithGames() {
  // let pronos: Prono[]  = { games: [], pronos: [] };

  // try {
  //   const res = await fetch("api/games", {
  //     method: "GET",
  //     credentials: "same-origin"
  //   });

  //   if (res.ok) {
  //     pronosAndGames = await res.json();
  //   } else {
  //     fetchError.set("error");
  //   }
  // } catch (error) {
  //   console.error(error);
  //   fetchError.set("error");
  // }
}
